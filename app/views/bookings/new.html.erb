<div class="container">
  <div class="card">
    <div class="card-header">
      <h2>New booking for <%= @flat.address%></h2>
    </div>
    <div class="card-body">
      <%= simple_form_for [@flat, @booking], html: { class: 'form-horizontal' } do |f| %>
        <div class="form-group form-inputs">
          <%= f.input :start_date, input_html: { class: 'form-control' } %>
        </div>
        <div class="form-group form-inputs">
          <%= f.input :end_time, input_html: { class: 'form-control' } %>
        </div>
        <div class="form-actions">
          <%= f.button :submit, "Create booking", class: 'btn btn-primary btn-block' %>
        </div>
      <% end %>
    </div>
  </div>
</div>
